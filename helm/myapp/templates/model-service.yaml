apiVersion: v1
kind: Service
metadata:
  name: model-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: model-service
  ports:
    - port: {{ .Values.modelService.port }}
      targetPort: {{ .Values.modelService.port }}
